<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:SaveUp.Views"
       x:Class="SaveUp.AppShell">
    <ShellContent Title="Home"
                  ContentTemplate="{DataTemplate views:MainPage}"
                  Route="MainPage" />
    <ShellContent Title="Verzicht hinzufügen"
                  ContentTemplate="{DataTemplate views:AddSavingPage}"
                  Route="AddSavingPage" />
    <ShellContent Title="Einsparungen"
                  ContentTemplate="{DataTemplate views:SavingsListPage}"
                  Route="SavingsListPage" />
</Shell>